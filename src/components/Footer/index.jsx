import styles from "./Footer.module.css";

function Footer() {
  return (
    <footer className={`py-5 ${styles.footer}`}>
      <small>Buscate bebidas</small>
    </footer>
  );
}

export default Footer;
